import 'package:flutter/material.dart';
import 'package:money_diagram/src/custom/custom_page.dart';

class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return CustomPage(
      body: Container(),
    );
  }
}
